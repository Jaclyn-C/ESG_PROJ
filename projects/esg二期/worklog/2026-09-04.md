# 2026-09-04 · esg二期 工作日志

## 16:20 esg二期 · 会话记录

> 会话 9/4 16:20 开始，跨零点收尾至 9/5 凌晨，主体工作在 9/4，故记本日。收工打卡于 9/5 10:08 执行。

### 新增

- **项目骨架**：`projects/esg二期/` 目录结构（inputs/screenshots、inputs/style-refs、understanding、outputs/{confirm,dev-spec,prototype,ppt,research,overview}、worklog/weekly、archive）；初始化 `status.md`；写 `inputs/README.md` 素材投放指南。
- **REQ-01 需求理解四轮升版**（现存 v4，v1/v2/v3 已归档 `archive/`）：
  - `understanding/REQ-01-全局AI助手-数据选择-v1.md`（草稿）：全局AI助手新增「数据选择」按钮——五层下钻（指标库→维度→主题→议题→指标）、当前子树搜索（仅匹配指标名）、指标多选、提交后 AI 一次性单表格返回；
  - v2（用户拍板 9 项待确认）：按钮排【指标信息采集表】后、仅管理员可见、层级定案五层、多选+单表格、搜索=停留层级子树、指标层只显示代码+名称、默认值等；
  - v3：追加年份选择（可不选默认当年）与维度选择（FII/分会/法人层级/策进组层级四选一、维度内多选、策进组两级、可不选默认 FII）——当时按全局查询条件实现；
  - `understanding/REQ-01-全局AI助手-数据选择-v4.md`（现版，草稿）：用户纠正「维度和时间都放在具体的指标里选，不是全局的」→ 改为指标级配置：每个已选指标条目各自配年份+维度，结果表行＝Σ(指标×各自对象)。

### 修改

- `inputs/prototype/html/chat-widget.js`（1771 → 2484 行，四轮修改，最终为 v4 指标级模型）：
  1. 实现【数据选择】按钮＋五层下钻弹窗＋子树搜索＋多选＋提交表格（2137 行时点）；
  2. 修复 bug：通用 agentbtn 点击处理器覆盖了新按钮绑定（改为先读 data-mode、无则跳过）；
  3. 加年份＋维度（v3 全局口径，2465 行时点）→ 返工为指标级配置条目（2489 行时点）；
  4. 收尾：应用户要求删除两句界面提示语（「未选指标库时不能选择下层内容…」「第 1 层 · 指标库：仅显示有效…共用同一棵演示树」）。
- `inputs/给开发的需求文档/ESG二期开发需求管理.md`（506 → 614 行，三轮更新）：6.2 章末新增 6.2.6「数据选择」小节，现为 v4 指标级口径（14 步主流程＋子树搜索 5 步＋维度二级弹窗 8 步＋27 条验收清单）；三轮均为节内改写，用户既有内容零改动（用户硬性要求）。

### 素材接收（用户自行放入 inputs/）

- `inputs/给开发的需求文档/ESG二期开发需求管理.md`（506 行，既有开发文档）；
- `inputs/prototype/html/` 现有可交互原型（含全局 AI 助手 chat-widget.js，原 1771 行）；
- `inputs/screenshots/`：image1.png（指标库列表）、image2.png（维度→主题→议题→指标四层结构）；会话后段补 分会.png、法人.png、策进组（包含策进组的二级组织）.png 三张（维度选项 mock 数据来源）。

### 交叉核对

- 每轮派生后主对话均执行（功能点 ↔ 文档步骤 ↔ 原型可点性 ↔ `node --check`），v4 全部通过。

### 关键决策

1. **v4 指标级口径**（用户纠正，原话："不对，维度和时间都放在具体的指标里选，不是全局的"）：年份与维度不再是全局查询条件，每个已选指标条目各自配置；不做配置＝当年＋FII。
2. **v2 九项拍板**：按钮排【指标信息采集表】后、仅管理员可见、五层层级定案、多选＋AI 单表格返回、搜索＝停留层级子树且仅匹配指标名、指标层只显示代码＋名称等。
3. **两条派生工作约定**：原型在 `inputs/prototype/html/` 原地改（不另出 outputs/prototype）；开发文档只增不改——新增小节内改写，用户既有内容零改动。
4. **chat-widget.js 绑定修复**：通用 agentbtn 点击处理器改为先读 data-mode、无则跳过，避免覆盖【数据选择】按钮绑定。

### 待办

- REQ-01 v4 仍为**草稿**，待用户验证原型后确认（点头才能标「客户已确认」）。
- 待确认 4 项：① 结果表格列集与列序 ② 维度二级弹窗是否提供搜索 ③ 权限过滤口径（建议遵循全局规则 6）④ 搜索空态表现。
- 待核实 1 项：G 策进组末项名称截图截断（疑为「G1商业道德-弊端防治处」）。
- 数据侧提醒：三张维度截图（分会/法人/策进组）含系统测试数据条目，正式上线前建议数据侧清洗（已写入开发文档 6.2.6）。
- 下一步：用户验证 v4 原型 → 确认 REQ-01 v4 → 后续如需可派生客户确认说明（PDF）与需求总览。
