# 2026-09-09 · esg二期 工作日志

## 09:45 esg二期 · 会话记录

> 本会话自 9/7 下午持续至 9/9 上午，主体工作发生在 9/7 下午～9/8（产物时间戳：数据核查产物 9/7 16:25/16:41、开发文档 9/8 11:17、终版文档 9/8 16:02），收工打卡于 9/9 09:45 执行——沿用 09-04 先例记入打卡当日文件。

### 新增

- **数据库核查产物**（9/7 下午，`outputs/数据核查/`，只读账号连测试库 test_csr_ai_db_config @ localhost:13306）：
  - `数据选择-取数查询.sql`（316 行，八节验证查询集，含「单指标单年份直查」可复用模板）；
  - `L-S-1-1-0001_2026年_全字段导出.csv`（指标全字段导出样本，供字段级核对）。
  - 核查范围：REQ-01 数据选择功能取数可行性——15 张相关表结构梳理（tb_flow_index 系 6 张、tb_sur 系 6 张、csr_bg_group / csr_legal_entity / csr_esg_group / tb_esg_grp_cent_org）；五层下钻（指标库→维度→主题→议题→指标）、四维度对象（FII/分会/法人/策进组）、三口径数值链路（法人 ENTITY / 分会 BG / 中央单位 CENT）全部验证可查。
- **终版需求文档**（9/8 16:02，163 行）：`inputs/给开发的需求文档/REQ-01-数据选择-需求说明-终版.md`——9 节独立文档，面向开发交付。合并开发文档中三块补丁、消除 4 处前后矛盾（如默认当年→默认最新有收集年份）；新增权限与技术边界（平台仓库实现 / 不经 LLM / 带 AI 角标 / AI 助手未接入预留挂载点 / Vue2 规范 / i18n 五语言）；明确行序与去重等默认行为；实现注意事项（既有 /flow/summary 接口带 COMPLETED 过滤不得直接复用、聚合复用 SurveyUtil.getIndexSummaryData、法人接口需分会 id 会不匹配、年份传参口径）。
- **代码仓库拉取**（9/8，工作台根目录非项目产物）：esg-ai 仓库拉至 `/Users/jaclyn/Desktop/workflow/PM-workflow/esg-ai`，用于代码↔数据库↔需求三方对照审查（FlowSummaryController 四维度汇总接口、SurveyUtil 聚合算法、idx-multi-path-allocate 派发页、仓库规范 REQ-YYYYMMDD-NN 分支 / i18n / Vue2）。

### 修改

- `inputs/给开发的需求文档/ESG二期开发需求管理.md`（590 → 626 行，9/8 11:17，均在 6.2.6 节）：
  1. 新增「数据口径与取数规则」10 条：数据范围不限表单状态 / 取值唯一性 / FII 按 aggMethod 汇总 / 法人含分会代填 / 分会填报主体 1 行 / 策进组两级 / 年份＝表单收集年份 / 无数据「—」占位 / 定性定量展示 / 有效库全显；
  2. 新增「年份与维度选项按指标实际收集范围裁剪」块（5 条终态）：维度选项按该指标历年派发并集裁剪、年份下拉只显示有收集表单的年份且默认最新、年份与维度互相独立无联动、FII 保留、从未收集空态；
  3. 主流程步骤 14 策进组表述修正（一级＝汇总结果 1 行、二级＝各 1 行明细不合并）。

### 关键发现（数据库核查）

1. G 策进组截图截断末项经数据库确认为「G1商业道德-弊端防治处」（**9/7 遗留待办就此解决**）；
2. 库内无现成 FII 汇总结果表（tb_sur_idx_summary 0 行）——FII 数值需按 aggMethod 现算；
3. 分会填报代表法人、按法人拆行同值落库（BG 行全带 legal_entity_id）；
4. 一期旧库 L（1103旧）valid=1 会在第 1 层出现且与新库重名——需数据侧置无效；
5. 同名指标在 I/L/SC 三库各一份，名称非唯一键——取数必须用指标代码而非名称。

### 关键决策

1. **MERGE 语义**对齐既有实现：非空值「；」拼接、不去重；
2. **DEPT 职能部门**：填报行计入所属法人口径，不设维度对象（维度固定 FII/分会/法人/策进组四个）——代码审查确认职能部门是法人路径内部的派发方式；
3. **默认年份口径统一**为「最新有收集年份」（推翻原「默认当年」，属终版文档消除的 4 处矛盾之一）；
4. **权限与技术边界落盘**：平台仓库实现、不经 LLM、界面带 AI 角标、AI 助手未接入先预留挂载点、遵循 Vue2 与 i18n 五语言规范。

### 9/9 答疑支持（无产物落盘）

- 代码走读答疑：getSuFiiIndexSummary 四层链路、底表 53 字段讲解；
- 附件机制确认：attachment_data 存 name+fileId、文件本体在文件中心；并产出 29 条含附件测试素材清单（含多附件行/多格式/双格式时间戳兼容用例）；
- 数据溯源查询支持。
- （按用户指示：零散的数据库字段问答/查询答疑细节不入账，仅记上述实质工作。）

### 待办

- **REQ-01 需求理解升 v7**：9/7–9/8 新增的数据口径与取数规则、年份维度裁剪规则目前只落在开发文档 6.2.6 与终版文档中，`understanding/REQ-01-…-v6.md`（9/7 10:32 后未动）尚未同步——不升版则下次派生开发文档会被静默覆盖；
- 待确认 2 项：① 旧库「1103旧」置无效（数据侧一条 update）② esg-ai 仓库分支编号（建议 REQ-20260908-01）；
- 开发问题清单 6 问已全部有定稿答案，可回复开发；
- REQ-01 v6（及升版后的 v7）仍为草稿，待用户整体确认后标「客户已确认＋日期」锁版；
- 后续如需：客户确认说明 PDF（confirm-doc-agent）、需求总览快照。
