# esg二期 · 当前状态

> 本文件是**快照**，由 worklog-agent 在每次收工打卡时**全量重写**（新会话开场必读）。
> 已完成的事项从这里移除（历史在 worklog）；仅保留"已确认版本"一行指纹作锚点。
> 最后更新：2026-09-05 10:08（对应 worklog/2026-09-04.md）

- **进行中**：
  - REQ-01 全局AI助手·数据选择 **v4（草稿）**：指标级模型——五层下钻（指标库→维度→主题→议题→指标）＋当前子树搜索（仅匹配指标名）＋指标多选；每个已选指标条目各自配年份（默认当年）＋维度（FII/分会/法人层级/策进组层级，默认 FII），AI 一次性单表格返回，结果表行＝Σ(指标×各自对象)。
  - 原型已同步 v4 口径：`inputs/prototype/html/chat-widget.js`（2484 行，按约定原地改，不另出 outputs/prototype）。
  - 开发文档已同步 v4 口径：`inputs/给开发的需求文档/ESG二期开发需求管理.md` 6.2.6 节（614 行；只增不改约定）。
  - 交叉核对 v4 已通过（功能点↔文档步骤↔原型可点性↔node --check）。
- **待确认**（4 项，均挂 REQ-01）：
  1. 结果表格列集与列序；
  2. 维度二级弹窗是否提供搜索（现口径：搜索仅作用于指标）；
  3. 权限过滤口径——指标数据是否按用户权限过滤（建议遵循全局规则 6，原型暂未实现）；
  4. 搜索空态表现。
  - 待核实 1 项：G 策进组末项名称截图截断（疑为「G1商业道德-弊端防治处」，见 `inputs/screenshots/策进组（包含策进组的二级组织）.png`）。
- **已锁定**：（暂无。REQ-01 v4 待用户验证原型并点头后方可标「客户已确认＋日期」；历史版本 v1–v3 已归档 `archive/`）
- **下一步**：
  1. 用户验证 v4 原型（五层下钻 / 子树搜索 / 逐指标年份＋维度配置 / 提交单表格）；
  2. 验证通过 → REQ-01 v4 标「客户已确认＋日期」锁版本；有问题 → 升 v5；
  3. 落实上述 4 项待确认＋1 项待核实；
  4. 后续如需：派生客户确认说明 PDF（confirm-doc-agent）与需求总览快照。
  - 备注：正式上线前数据侧需清洗三张维度截图（分会/法人/策进组）中的系统测试数据条目（已写入开发文档 6.2.6）。
