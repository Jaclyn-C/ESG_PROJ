# REQ-01 全局AI助手·数据选择 需求理解 v7

> **状态**：客户已确认（2026-09-11）
> **上一版**：v6（草稿，2026-09-07，已归档至 archive/）
> **项目**：esg二期

## 本版变更（v6 → v7）

v7（2026-09-11）：依据 9/7–9/8 会话拍板**补账同步**——数据口径与取数规则 10 条、年份与维度选项按实际收集范围裁剪 5 条、步骤 14 策进组口径修正，原文先落盘于开发文档 6.2.6（`inputs/给开发的需求文档/ESG二期开发需求管理.md`）与 REQ-01 终版文档（`inputs/给开发的需求文档/REQ-01-数据选择-需求说明-终版.md`），本次同步进需求理解以防下次派生开发文档时被静默覆盖；除下述同步外**无其他变更、内容零新增**（全部照抄已落盘文字；两处口径有差异时以开发文档 6.2.6 为准，差异点见本节末尾）。

- **状态（2026-09-11）· 用户确认锁版**：依据 2026-09-11 当日对话，用户口头确认 v7 锁版；状态由「草稿」转为「客户已确认（2026-09-11）」。本版自此锁死，后续任何变更须升 v8。

- **新增 · 第 10 节「数据口径与选项范围规则」（补账同步）**：
  - **10.1 数据口径与取数规则（结果表格「数值」列的取数依据）· 10 条**（9/7 拍板，照抄开发文档 6.2.6）：①数据范围＝取已填报的指标值（填报行存在且值非空即计入，不限表单状态，进行中／已完成均可）；②取值唯一性（同一指标×年份×维度对象仅一份填报）；③FII 整体口径＝该指标该年份全部有效填报，按指标配置时已设定的汇总方式汇总合成；④法人口径＝归属该法人的全部填报行（含法人自填＋分会代填；分会一次填报代表其覆盖的全部法人，按法人拆行落库、各行同值）；⑤分会口径＝该分会作为填报主体提交的填报，结果中 1 行、不按覆盖法人展开不重复计；⑥策进组口径（勾一级＝该组整体口径 1 行、汇总合成同第 3 条；勾选的每个二级组织＝各 1 行）；⑦年份下拉的值＝表单的收集年份；⑧无数据＝保留该行、数值列「—」占位；⑨定量指标显示数值、定性指标显示填报文本原样，单位取指标库维护的单位；⑩第 1 层显示所有「有效」状态的指标库。
  - **10.2 年份与维度选项按指标实际收集范围裁剪 · 5 条**（开发文档标注【新增 2026-09-08】，照抄）：维度页签选项不取全量、按该指标实际派发过的对象裁剪（未派发不显示不可选；派发范围＝历年收集表单派发记录的并集）；年份下拉仅显示该指标实际存在收集表单的年份，**不选时默认取最新有收集记录的年份**、下拉占位动态显示该默认年份（如「默认 2025 年」）；年份与维度两项配置互相独立、无联动，组合查不到数据按无数据处理（「—」占位）、不做勾选清理或提示；FII 页签不受裁剪、永远显示「FII」；派发未填仍显示（裁剪依据是派发范围而非是否有填报值）；从未发起过收集的空态（年份下拉无可选项、四页签显示「该指标尚未发起过收集」，可直接提交，按无数据处理：FII 1 行、数值「—」）。
  - **优先级声明（沿用开发文档 6.2.6 原文口径）**：10.2 优先于「全量列表」选项口径与「不选默认当年」口径——落到本 REQ 即：正文中所有「（不选/未配置）默认当年（当前＝2026）」的表述（FP-4、FP-6、流程 A 步骤 11/13、数据项清单「年份（指标级）」行、第 5 节「条目年份未选」行）以及第 9 节附录 mock 全量列表（仅供原型 mock 取用），凡与 10.2 冲突之处**以 10.2 为准**；v6 既有文字按"只增不改"原则保留原样、未逐处改写。
- **修改 · 步骤 14 策进组口径表述修正**（照抄开发文档 6.2.6 主流程步骤 14）：策进组一级由 v6「1 行，不展开**不求和**」修正为「该策进组整体口径的**汇总结果** 1 行，不展开二级组织（数值取数见 10.1 第 6 条）」，并补「勾选二级组织＝每个组织各 1 行明细，选几个出几条、不合并」。**同一句口径表述在 FP-5 与第 5 节边界表「多选数据语义（无累加）」行一并同步修正**（三处原为同一句话，仅改此一句、其余文字不动——略超"仅步骤 14"的字面范围，为保持文档内部一致，特此注明）。
- **派生约定补记（第 8 节 B 新增一条，防覆盖）**：后续派生更新开发文档 6.2.6 时，其「数据口径与取数规则」「年份与维度选项按指标实际收集范围裁剪」两个规则块必须保留，口径不得删除或回退。
- **差异点记录（开发文档 6.2.6 vs 终版文档；按任务约定以开发文档 6.2.6 为准落盘，以下终版差异仅记录、未采纳）**：
  1. 口径第 3 条：终版多注「（aggMethod）」字样。
  2. 口径第 4 条（法人口径）：终版为**三类**（①法人自填 ②分会代填 ③**该法人下职能部门代填**，并明确职能部门不显示为维度对象、其填报行计入所属法人口径并参与 FII 汇总，标注 2026-09-08 定稿）；开发文档仅两类（法人自填＋分会代填）。本版按开发文档两类落。
  3. 口径第 6 条（策进组）：终版明确一级数值合成「范围仅限该组策进组收集路径、不含法人/分会口径行」；开发文档写「数值取该组整体口径的填报，汇总合成同第 3 条」。本版按开发文档落。
  4. 口径第 10 条：终版多「（以库的有效状态为准）」。
  5. 裁剪第 5 条（空态）：终版多「年份列『—』」（从未发起收集时结果行年份列显示「—」）；开发文档未写。本版按开发文档落。
  6. 编排差异：开发文档裁剪块 5 条（「年份与维度独立无联动」并在第 1 条内）；终版拆 6 条（独立成其第 5 条）。实质一致，本版按开发文档 5 条编排。
- **不变**：v6 其余全部内容原样保留——FP-1～FP-8（除上述策进组一句修正）、流程 A/B/C/D（除步骤 14 一句修正）、数据项清单、状态与边界（除一行修正）、本次不包含、待确认清单（维持清零状态）、派生工作约定（仅新增一条补记）、第 9 节附录。

## 1. 背景与目标

平台现有「全局 AI 智能助手」（悬浮对话窗口，客户需求文档 6.2，状态【开发中】），已具备三种模式（智能问答 / 报告生成 / 指标信息采集表）以及文字输入、📎 本地附件上传、【🗂】知识库引用等输入方式。本需求在该助手中新增「数据选择」功能：用户按指标层级逐层下钻（或搜索）选定指标；**每个已选指标各自配置年份与维度**，维度支持**跨维度多选**（同一指标可同时按 分会/法人/策进组 等不同维度圈定多个对象），以「指标名 + 层级 + 该指标自己的年份 + 该指标自己的跨维度对象」确定具体的数据；一次性提交后由 AI 以单个表格返回查询结果（选择与返回一一对应、无累加），表格支持**全屏**查看；维度选择弹窗提供**选中可视化**（着色选中态＋按维度着色的已选总览 chips）。

指标层级共 5 层，参照截图（`inputs/screenshots/`）：
- **image1.png**＝指标库列表（第 1 层）：列为 # / 指标库 / 是否有效 / 指标库详情（配置）/ 操作（编辑、删除）；共 7 个指标库（含 2 个「无效」旧库）。**本功能第 1 层仅显示 5 个「有效」指标库：E维度/S维度/G维度/SC/O维度指标库。**
- **image2.png**＝指标库详情：内部四层结构 **维度 → 主题 → 议题 → 指标**（示例树：环境（维度）→ 环境政策与管理体系（主题）、能源（主题）→ 各自议题 → 指标）；右侧表格含 指标代码（格式如 E-E-14-1-0001）/ 指标名称 / 指标描述 等。**本功能在指标层仅取 指标代码 + 指标名称 两个字段展示。**

维度选项 mock 来源（三张系统管理页截图，详见第 9 节附录）：`分会.png`（13 项扁平列表）、`法人.png`（10 项，法人代码+法人名称）、`策进组（包含策进组的二级组织）.png`（两级：3 个策进组＋各二级组织）；FII 仅 1 个选项「FII」。

与现有全局规则的衔接：
- **全局规则 1（AI 标注）**：「数据选择」本身是用户主动选择行为；AI 返回的查询结果表格（含全屏视图）属 AI 生成内容，须按现行 AI 标注规则执行（"⚠ AI生成/提取，请人工核实"）。
- **全局规则 6（AI 权限可见性）**：本功能**不考虑任何权限**（不做权限过滤，9/7 用户拍板）；该决定**不写入开发需求文档**（仅 REQ 记录供内部追溯，见第 8 节 B 补记）。
- **全局规则 7（Agent 功能按角色开放）**：【数据选择】与【报告生成】【指标信息采集表】一致，**仅对管理员可见**（v2 用户拍板）。

## 2. 功能点

- **FP-1 【数据选择】按钮（仅管理员）**：位于助手输入区上方的模式/功能按钮区，排在【指标信息采集表】之后；仅管理员可见，普通用户不显示该按钮（参照全局规则 7 先例：无对应模式与提示）。完成标志：管理员能在按钮区看到并点击【数据选择】。
- **FP-2 五层下钻选择**：指标库（第 1 层）→ 维度（第 2 层）→ 主题（第 3 层）→ 议题（第 4 层）→ 指标（第 5 层）；未选指标库不能选下层；指标层列表项仅显示 指标代码 + 指标名称。完成标志：用户能从指标库一路下钻到指标并勾选。
- **FP-3 子树搜索**：在当前停留层级可搜索该层级节点子树内的所有指标（跨下级直达），停留越深范围越窄；仅匹配指标名称。**搜索仅作用于指标，不扩展到维度选项**（维度二级弹窗不提供搜索，9/7 定稿）。完成标志：输入关键词返回子树内名称匹配的指标并可选中。
- **FP-4 多选与批量提交（指标级口径）**：指标可多选；**每个指标各自携带其年份与跨维度配置**一次性提交；未做任何配置的指标按默认值（当年＋FII）提交。完成标志：一次提交一组各带自身年份/维度口径的指标查询。
- **FP-5 结果表格一次性返回（一一对应，无累加）**：AI 对话框对本次提交的全部指标**一次性**返回查询结果，以**一个表格**承载；**行构成＝选择与返回一一对应（无累加）**：每个所选维度对象对应一条数据；同一指标选 N 个对象（同维度内或跨维度）＝N 行，每行为经该维度从数据库取到的一条数据；无数值加总、无合并/汇总行；策进组一级＝该策进组整体口径的**汇总结果** 1 行，不展开二级组织，勾选二级组织＝每个组织各 1 行明细、选几个出几条、不合并（数值取数见 10.1 第 6 条；v7 依开发文档 6.2.6 步骤 14 修正原"不展开不求和"表述）；维度未选的指标按 1 个默认对象 FII 计；每个指标行的年份列、维度对象列取该指标各自的配置；**维度对象列＝单列定稿（9/7 拍板）**：单元格显示「维度名 · 对象名」（如「分会层级 · CESBG」）以区分所属维度，不拆两列；**列集与列序定稿（9/7 拍板，按现状）：指标代码 / 指标名称 / 年份 / 维度对象 / 数值 / 单位（此顺序，无数据来源列）**；**指标无查询结果时保留该行、数值列以「—」占位（9/7 定稿）**。完成标志：会话中出现承载本次全部提交指标结果的单一表格。
- **FP-6 指标级年份选择**：已选指标区内，**每个已选指标条目内**各自提供年份选择（默认口径：年份下拉，mock 年份范围取近年若干年）；**可选可不选，不选默认当年（当前＝2026）**；各指标年份可互不相同。完成标志：年份按指标各自生效，未选时该指标按当年处理。
- **FP-7 指标级维度选择（跨维度多选＋选中可视化）**：**每个已选指标条目内**各自提供【维度选择】入口，点击弹出二级弹窗，**弹窗内四个维度的选项可跨维度累计勾选**——同一指标可同时选 FII＋分会层级若干对象＋法人层级若干对象＋策进组层级若干对象（**四维度全部可共存，不互斥**；每个维度内仍多选）；法人选项显示 法人代码＋法人名称；策进组两级——可只勾选一级策进组（＝覆盖该组、不选二级组织），也可勾选二级组织；FII 为整体口径；**可不选，全部不选时默认 FII**；确认结果只落到当前配置的这个指标上，条目内回显跨维度汇总（如「分会×2、法人×1、策进组×1」），可清除。**选中可视化**：① 页签内已勾选行显示着色选中态（高亮底色＋勾选标记，与主选择弹窗选中行样式一致）；② 弹窗内「已选对象」总览区以彩色 chips 展示全部已选对象——按所属维度着色（FII/分会/法人/策进组各一色），chip 文字含维度与对象名，可单独【✕】移除，随勾选实时刷新、跨页签可见；③ 页签数量角标保留。完成标志：每个指标能独立跨维度圈定对象；任意时刻在页签内与总览区都能看清已选内容并能移除；未选时该指标按 FII 处理。
  - **FII 可与其他维度混选（9/7 拍板，推翻原「互斥」默认口径）**：四个维度之间、维度内之间都可多选，所有选择都不互斥——FII 可与分会/法人/策进组对象同时勾选、同时生效（例：同时勾 FII＋分会·CESBG＋法人·某法人＝3 行：FII 整体 1 行＋CESBG 1 行＋该法人 1 行，仍一一对应、无累加）；唯一保留的默认：**全部不选**时按 FII 整体口径查询（该指标 1 行）。
- **FP-8 结果表格全屏**：指标数据查询结果卡片的表格提供【全屏】按钮；点击后该表格**全屏展示**；全屏内容＝该卡片内的表格本身（表头、全部行，列集同 FP-5：指标代码/指标名称/年份/维度对象/数值/单位），保留「⚠ AI生成/提取，请人工核实」标注与表脚注记；全屏实现方式＝**覆盖层**（9/7 定稿，现状）；退出＝【✕】按钮或 ESC。完成标志：任一结果表格可进入与退出全屏，内容与卡片内表格一致。

## 3. 交互流程（点击级）

> 界面形态默认口径（可改）：数据选择为**主弹窗**；维度选择为**叠在其上的二级弹窗**，风格对齐现有弹窗（如【🗂】知识库选择弹窗）。**年份与维度不在全局条件行，而在每个已选指标条目内**（v4 纠正确立）；**维度为跨维度多选**（v5 变更一）；**多选返回一一对应、无累加**（v5 9/7 澄清）。

### 流程 A：五层下钻、已选区逐指标配置、提交（对应原型位置：全局 AI 助手 widget · 输入区上方按钮区 + 数据选择弹窗）

1. 管理员用户在平台任一页面右下角打开全局 AI 智能助手（沿用 6.2.1 既有交互）。普通用户：输入区上方按钮区不出现【数据选择】按钮。
2. 助手输入区上方的模式/功能按钮区（现有【报告生成】【指标信息采集表】等）中，【指标信息采集表】之后显示【数据选择】按钮。
3. 管理员点击【数据选择】，弹出数据选择弹窗。
4. 弹窗展示第 1 层「指标库」列表：仅显示 5 个「有效」指标库——E维度指标库、S维度指标库、G维度指标库、SC指标库、O维度指标库（2 个「无效」旧库不显示）。
5. 未选择指标库时，不能选择下层内容。
6. 选定某个指标库后，显示该库的维度列表（第 2 层）。**原型 mock：无论选择哪个指标库，下钻内容都复用同一棵 环境→主题→议题→指标 树**（环境维度下含 环境政策与管理体系、能源 等主题，与 image2 一致）。
7. 选择某维度后，显示其下主题列表（第 3 层）；选择某主题后，显示其下议题列表（第 4 层）；选择某议题后，显示其下指标列表（第 5 层，即 image2 右侧表格中的指标）。
8. 指标层列表项仅显示两项信息：指标代码 + 指标名称（如 `E-E-14-1-0001 能源消耗总量`）；不显示指标描述、维度/主题/议题归属等其他字段。
9. 用户勾选指标，可勾选多个（多选）。
10. 每个勾选的指标进入弹窗内**已选指标区**，成为一条**可配置条目**；条目信息：指标代码＋指标名称，以及该指标自己的 ① 年份选择 ② 【维度选择】入口 ③ 移除操作。
11. 用户在条目内各自配置：**年份**（默认口径：年份下拉，mock 近年若干年；可不选，不选默认当年，当前＝2026）；**维度**（点击条目内【维度选择】进入流程 C，**可跨维度累计勾选**多个对象，确认结果只落到当前条目并回显跨维度汇总，如「分会×2、法人×1、策进组×1」；可不配置，不选默认 FII）。不同条目可互不相同。
12. 条目可移除；移除时该指标的年份/维度配置随之丢弃。
13. 用户提交（一次提交全部已选指标，每个指标携带各自年份与跨维度对象；不做任何配置＝全部按 当年＋FII；提交按钮命名与位置随原型定）。
14. AI 对话框**一次性**返回查询结果：以**一个表格**承载，行构成＝选择与返回一一对应——每个所选维度对象对应一条数据；同一指标选 N 个对象（同维度内或跨维度）＝N 行，无数值加总、无合并/汇总行；策进组一级＝该策进组整体口径的**汇总结果** 1 行，不展开二级组织（数值取数见 10.1 第 6 条）；勾选二级组织＝每个组织各 1 行明细，选几个出几条、不合并（v7 依开发文档 6.2.6 步骤 14 修正原"不展开不求和"表述）（例：同一指标选 3 个不同维度对象即显示 3 行该指标，每行是经不同维度从数据库取到的一条数据——用户口述例证）；年份列/维度对象列按各指标各自配置，维度对象列＝单列，显示「维度名 · 对象名」（9/7 拍板定稿，不拆两列）；列集与列序见 FP-5（9/7 定稿）；指标无查询结果时保留该行、数值列显示「—」（9/7 定稿）；带 AI 标注。

### 流程 B：子树搜索（与流程 A 并用，作用于当前停留层级）

1. 用户下钻至任一层级（如主题层）时，在搜索框输入关键词。
2. 系统在**当前停留层级节点的子树内**检索所有指标（跨下级直达）：停在主题层即可搜到该主题下所有指标；停留层级越深，检索范围越窄；无匹配结果时显示「暂无匹配指标」（9/7 定稿）。
3. 仅匹配**指标名称**，不匹配指标代码。
4. 用户在搜索结果中勾选目标指标，效果与逐层勾选相同（同样进入已选指标区成为可配置条目），可多选、可一并提交。
5. 搜索仅作用于指标选择，不覆盖年份与维度选项；**维度二级弹窗不提供搜索（9/7 定稿）**。

### 流程 C：维度选择（二级弹窗 · 跨维度累计勾选＋选中可视化；入口与落点＝当前配置的指标条目）

1. 用户在**某条已选指标条目内**点击【维度选择】，弹出二级弹窗（叠在数据选择弹窗之上，弹窗保持打开；风格对齐现有弹窗）。
2. 弹窗提供四个维度的页签/选项区：FII / 分会层级 / 法人层级 / 策进组层级，**页签带数量角标**（显示该维度已选对象数，角标保留）；**跨维度累计勾选**：不切换作废——在同一指标上可同时勾选 FII＋分会层级若干＋法人层级若干＋策进组层级若干（四维度含 FII 全部可共存，不互斥），每个维度内仍多选。
3. **分会层级**：单层列表，选项为各分会（mock 13 项，见第 9 节附录），可多选。
4. **法人层级**：单层列表，选项为各法人（mock 10 项，每项显示 法人代码＋法人名称），可多选。
5. **策进组层级**：两级结构——可直接勾选一级策进组（E策进组 / S策进组 / G策进组，＝覆盖该组、不限定二级组织）；也可勾选其下二级组织（如 E1气候行动…，mock 见附录），可多选；允许只选一级、不选二级。
6. **FII**：仅 1 个选项「FII」，**可与其他维度的对象同时勾选、同时生效**（不互斥；例：同时勾 FII＋分会·CESBG＋法人·某法人＝3 行：FII 整体 1 行＋CESBG 1 行＋该法人 1 行，一一对应、无累加）；唯一保留的默认：**全部不选**时按 FII 整体口径查询（该指标 1 行）。
7. **页签内选中态**：已勾选对象的行显示明显着色选中态——高亮底色＋勾选标记，与主选择弹窗选中行样式一致；切换到其他页签再切回，选中态保持可见。
8. **跨维度已选总览区**：弹窗内提供「已选对象」总览区——每个已选对象一枚彩色 chip，**按所属维度着色**（FII/分会/法人/策进组各一色），chip 文字含维度与对象名（如「分会 · CESBG」「法人 · 富联科技(晋城)」）；随勾选**实时刷新**，**跨页签可见**（当前页签看不到的已选对象也在此呈现——解决「切走页签看不到选了什么」）。
9. **chip 移除**：点某枚 chip 上的【✕】单独移除该对象，对应页签内该行取消选中、角标数字减一，总览区同步刷新。
10. **可不选**：该指标不勾选任何维度对象时，默认 FII；确认后关闭二级弹窗，**选择结果只落到当前配置的这个指标条目**：条目内回显跨维度汇总（如「分会×2、法人×1、策进组×1」），可清除；清除后该指标回到默认 FII。其他指标条目不受影响。

### 流程 D：结果表格全屏（会话区 · 查询结果卡片）

1. 查询结果卡片内的表格提供【全屏】按钮。
2. 用户点击【全屏】，该表格进入全屏展示（**覆盖层全屏，9/7 定稿**）。
3. 全屏内容＝该卡片内的表格本身：表头＋全部行（列集同 FP-5：指标代码/指标名称/年份/维度对象/数值/单位，无数据来源列），保留「⚠ AI生成/提取，请人工核实」标注与表脚待确认注记。
4. 用户点击【✕】或按 ESC 退出全屏，回到会话区原位（表格内容与状态不变）。

## 4. 数据项清单（语义级）

| 数据项 | 业务含义 | 出现位置 | 来源方式 | 格式/约束 |
|---|---|---|---|---|
| 指标库 | 第 1 层选择项 | 数据选择弹窗第 1 层 | 平台指标库配置（image1） | 仅显示「有效」库，共 5 个（E/S/G/SC/O 维度指标库） |
| 维度（指标层级） | 第 2 层选择项 | 第 2 层 | 指标库详情（image2） | 如：环境 |
| 主题 | 第 3 层选择项 | 第 3 层 | 指标库详情（image2） | 如：环境政策与管理体系、能源 |
| 议题 | 第 4 层选择项 | 第 4 层 | 指标库详情（image2） | image2 示例树中议题层的各项 |
| 指标 | 第 5 层（最终选定项） | 第 5 层列表 / 搜索结果 | 指标库详情（image2 右侧表格） | 列表项仅显示 指标代码（格式如 E-E-14-1-0001）+ 指标名称，其余不显示；可多选 |
| 搜索关键词 | 在当前层级子树内检索指标 | 搜索框 | 用户输入 | 仅匹配指标名称；范围为当前停留层级节点的子树 |
| 已选指标条目 | 一个已选指标及其自身配置的载体 | 数据选择弹窗·已选指标区 | 用户勾选生成 | 条目＝指标代码＋指标名称＋年份（下拉）＋【维度选择】入口＋移除；条目间配置互相独立 |
| 年份（指标级） | 该条目的数据年份 | 已选指标条目内 | 年份下拉（默认口径，mock 近年若干年） | 每个条目各自设置；可不选，不选默认当年（当前＝2026）；条目间可不同 |
| 维度对象集合（指标级·跨维度） | 该条目跨各维度勾选的全部对象 | 维度二级弹窗（由条目内入口打开）→ 条目内回显 | 各维度管理页数据（3 张截图 mock；FII 固定 1 项） | 跨维度累计多选（分会若干＋法人若干＋策进组若干）；法人显示 法人代码＋法人名称；策进组两级可选（只选一级＝覆盖整组）；FII 可与其他维度对象混选、全部可共存（不互斥，9/7 拍板）；回显＝跨维度汇总（如「分会×2、法人×1、策进组×1」），可清除；只作用于当前条目 |
| 页签数量角标 | 各维度页签已选对象数提示 | 维度二级弹窗·各页签 | 由勾选状态计算 | 保留（v6 前已有）；随勾选/移除实时增减 |
| 已选对象 chip（总览） | 单个已选对象的跨页签可视化 | 维度二级弹窗·「已选对象」总览区 | 由勾选状态生成 | 一对象一 chip；按所属维度着色（FII/分会/法人/策进组各一色）；文字含维度与对象名；可单独【✕】移除；随勾选实时刷新、跨页签可见 |
| 查询结果表格 | AI 对本次全部提交一次性返回的结果 | AI 助手会话区（结果卡片） | AI 查询（原型 mock；每行＝经该维度从数据库取到的一条数据） | 单一表格；行＝选择与返回一一对应（每个所选维度对象对应一条数据；同一指标 N 个对象＝N 行；无累加/汇总行）；年份列/维度对象列按各指标各自配置；维度对象列＝单列「维度名 · 对象名」（9/7 拍板定稿，不拆两列）；**列集与列序定稿：指标代码/指标名称/年份/维度对象/数值/单位（此顺序，无数据来源列，9/7 拍板）；指标无查询结果时保留该行、数值列「—」（9/7 定稿）**；带 AI 标注（全局规则 1） |
| 全屏视图 | 结果表格的全屏展示态 | 结果卡片【全屏】按钮触发 | 同上（卡片内表格本身） | 覆盖层全屏（9/7 定稿）；含表头与全部行（列集与列序同上）；保留 AI 标注与表脚注记；【✕】/ESC 退出 |

## 5. 状态与边界

| 场景 | 表现 |
|---|---|
| 普通用户（非管理员） | 不显示【数据选择】按钮（参照全局规则 7 先例：无对应模式与提示） |
| 未选指标库 | 不能选择下层内容（必须先选指标库） |
| 条目年份未选 | 该指标按当年（当前＝2026）查询，结果行年份列按当年展示 |
| 条目维度未选 / 已清除 | 该指标按默认 FII：结果中该指标按 1 个默认维度对象「FII」展开 |
| 同一提交中不同指标配置不同 | 允许：各指标年份/维度互不相同，各行按各自配置返回 |
| 同一指标跨维度对象共存 | 允许：一次提交中同一指标可携带多个维度（含 FII）的多个对象，各对象各出一行；所有选择都不互斥、可任意组合勾选（9/7 拍板："所有选择都不互斥"） |
| 多选数据语义（无累加） | 选择与返回一一对应：同维度内或跨维度多选 N 个对象＝N 行各自数据，无数值加总、无合并/汇总行；策进组一级＝该策进组整体口径的汇总结果 1 行、不展开二级组织，勾选二级组织＝每个组织各 1 行明细、不合并（v7 修正，取数见 10.1 第 6 条；9/7 用户澄清："无论是跨维度多选，还是维度内的多选，都不需要累加，选择什么就返回什么"） |
| 维度页签切换 | 已选状态保持：切走再切回，行选中态、页签角标、已选总览 chips 均不丢失 |
| 移除已选对象 chip | 仅移除该对象：对应页签行取消选中、角标减一，其他已选对象与总览区不受影响 |
| 移除指标条目 | 该指标的年份/维度（含全部跨维度对象）配置随之丢弃（不可恢复） |
| 策进组只勾选一级 | 该指标口径＝该策进组整体（不限定二级组织），计 1 个对象 |
| 全屏中退出 | 【✕】或 ESC 退出全屏，回到会话区原位，表格内容不变 |
| 搜索无匹配结果 | 显示「暂无匹配指标」（9/7 定稿） |
| 指标库/指标/维度数据加载中 | 待确认 |
| 数据加载失败 | 待确认 |
| 权限过滤 | 本功能不考虑任何权限：不对指标/维度对象做权限过滤（9/7 拍板）；此决定不写入开发需求文档（仅本 REQ 记录供内部追溯） |
| 提交的指标无查询结果 | 保留该行，数值列显示「—」（9/7 定稿） |

## 6. 本次不包含

- 时间粒度仅到「年份」：不含半年/季度/报告期等其他时间粒度（用户仅提出年份）。
- 查询条件仅＝指标＋（指标级）年份＋（指标级·跨维度）维度：其他查询条件未提出，不在本版。
- 数据来源标注：结果表格「数据来源」列已删除（9/7 拍板）；如需出处溯源另行按全局规则 8 在回答文字层面处理，不在本版表格内。
- 权限过滤：本功能不考虑任何权限（9/7 拍板）；此决定不写入开发需求文档（仅 REQ 内部记录，见第 8 节 B 补记）。
- 指标库本身的管理功能（image1 操作列的编辑/删除、指标库详情配置）——平台既有能力，本需求只做「选择」。
- 各维度管理页本身的功能（分会管理/法人管理/策进组管理页面）——截图仅作选项数据示例来源。
- 全屏视图内的表格编辑、排序、筛选等增强操作（本版全屏仅放大查看；如需增强另行提出）。
- 后端真实数据逻辑：原型阶段选任一指标库均复用同一棵 环境→主题→议题→指标 mock 树；维度选项按三张截图 mock；查询结果表格（含跨维度多行）为原型模拟。
- 助手既有功能的改动：三种模式、📎 上传、【🗂】知识库引用、历史对话管理等维持现状。

## 7. 待确认问题

（截至 2026/9/7，v6 待确认事项已**全部定稿**，清单清零——最后一项「搜索空态表现」由用户在开发文档中落定为「无匹配结果时显示暂无匹配指标」。v7 补账同步不引入新待确认。）

> （移出记录——均已 9/7 拍板：维度对象列＝单列「维度名 · 对象名」；FII 可与其他维度混选（所有选择不互斥）；结果表格列集与列序＝指标代码/指标名称/年份/维度对象/数值/单位（按现状定稿）；指标无数据＝保留该行、数值列「—」；维度弹窗搜索＝不加；权限过滤＝不考虑任何权限（且不写入开发文档）；全屏＝覆盖层；搜索空态＝「暂无匹配指标」。）

## 8. 派生工作约定（三件套派生 agent 必读）

> A、B 为 2026/9/4 用户明确的工作约定；C 为 v6 变更引出的派生验收要点。

### A. 原型宿主与修改方式

- 现有可交互原型位于 `projects/esg二期/inputs/prototype/html/`，其中 `chat-widget.js` 为全局 AI 助手 widget（被各页面共享引用）；【数据选择】按钮及五层下钻/搜索/已选区逐指标配置/跨维度选择（含选中可视化）/提交/结果表格（含全屏）交互均在该 widget 内实现。
- 用户已明确授权：**可直接在该位置修改原型，或按需移动位置**。
- 本工作台「inputs 对 agent 只读」的约定**对此原型例外**——仅限此原型；inputs 内其他素材仍只读。

### B. 开发需求文档同步（硬性约束）

- 需求确认落地后，须同步更新 `inputs/给开发的需求文档/ESG二期开发需求管理.md`（给开发看的正式文档），将本需求按该文档既有体例补入（建议挂接 6.2 全局 AI 智能助手章节下新增子节）。
- **硬性约束：只允许在对应位置新增内容，禁止修改、删除、重排任何已有内容。**
- **补记（2026/9/7）· 开发文档不罗列原型/演示数据**：交互原型的数据均为演示 mock（含测试条目），面向开发的需求文档**不罗列原型数据清单**——不枚举选项全列表（如分会 13 项、法人 10 项、策进组各二级组织全表）、不写死条目数量；只写规则与结构（层级关系、显示格式、数据来源模块，如「选项来自分会管理页」）；表达可用示例，**每处至多一两个**；真实数据以系统各管理模块实际配置为准。（用户原话："原型的内容仅作展示为了给开发理解，原型的数据都是随便写的，不是真实的数据，所以不需要写进去需求管理的md文档中。……你可以举例子去表达你的意思，但是不需要罗列原型数据，如果想举例子只需要挑选一两个"）本 REQ 第 9 节附录仅供交互原型 mock 取用，**不进入开发文档**。
- **补记（2026/9/7）· 开发文档 6.2.6 以用户版本为准**：用户已按上述口径手工修订开发文档 6.2.6 的一部分（并手工删除 6.2.6 开头的「新增内容内部说明」blockquote、将界面形态定为弹窗·默认口径），主对话已代为完成剩余清理（步骤 5、流程 C ③④⑤、维度选项规格块、验收清单 FP-2/FP-7 相关条目）。**后续派生一律以用户修订后的 6.2.6 为准。**
- **补记（2026/9/7 拍板）· 权限决定不写入开发文档**：用户拍板本功能「不考虑任何权限」，且明确「不需要把这个内容写进需求管理内」——开发需求文档**不写权限过滤相关内容**（主对话已从 6.2.6 移除权限相关表述）；本条仅记录于本 REQ 供内部追溯。
- **补记（2026/9/7）· 每条规则只定义一次**：开发文档中一条规则只在其归属位置定义一处（功能说明可保留一句概览），其余位置引用不复述；杜绝同一规则在多个步骤块逐字重复。
- **补记（2026/9/7）· 过程记录不进开发文档正文**：拍板日期、「定稿」「移出待确认」「推翻原口径」「REQ-01 vX」等版本过程字样一律不写入开发文档——历史只存在于需求理解的版本变更记录；开发文档只呈现干净的最终态。
- **补记（2026/9/7）· 6.2.6 以用户精简版为基线，不再生成验收清单**：用户已删除 6.2.6 的验收清单并手工精简全文，主对话已完成最后去重收口（11 处编辑）；**后续对 6.2.6 的派生更新以用户精简后的版本为基线，不再重新生成验收清单**（除非用户要求）。
- **补记（2026/9/11）· 第 10 节规则为派生必含内容（防覆盖）**：本 REQ 第 10 节（10.1 数据口径与取数规则 10 条、10.2 年份与维度选项按指标实际收集范围裁剪 5 条）为 9/7–9/8 拍板口径，开发文档 6.2.6 中已有对应两个规则块；后续任何派生更新 6.2.6 时**必须保留这两个规则块**（措辞可整理，口径不得删除、不得回退），防止静默覆盖。

### C. v6 派生验收要点（历史保留——用户已删除 6.2.6 验收清单并确立不再生成，见 B 补记；以下转为原型/测试自测参考，不写入开发文档）

1. 维度弹窗页签内：已勾选对象行着色选中态可见（高亮底色＋勾选标记），切换页签再切回不丢失。
2. 已选对象总览区：chips 按所属维度着色（FII/分会/法人/策进组各一色），随勾选实时更新，跨页签可见。
3. 已选对象 chip 可单独【✕】移除，且联动页签行取消选中与角标减一。
4. 结果表格列集与列序＝指标代码/指标名称/年份/维度对象/数值/单位（此顺序，**无数据来源列**）。
5. 指标无查询结果：行保留、数值列显示「—」。
6. 全屏＝覆盖层实现，【✕】/ESC 可退出。

（注：权限相关不写验收条目——本功能不考虑权限，且该决定不进入开发文档，见 B 补记。）

## 9. 附录：各维度选项 mock 数据（来自 screenshots，原型直接取用）

> 三张截图均为系统管理页，作为各维度选项的数据示例；含系统测试数据，原样记录。

### 9.1 FII（无截图，用户口述）

- 仅 1 个选项：**FII**（整体口径；可与其他维度对象同时勾选、同时生效，不互斥——9/7 拍板）。

### 9.2 分会层级（分会.png · 分会管理页 · 扁平单层 13 项）

Angel-test、beck-分会、CESBG、CNSBG、iPEBG-iPEG、iPEBG-PMEG、SEG、TARG、TEST、TEST2、WTT-test、Xuyu-Test、zhushen

### 9.3 法人层级（法人.png · 法人管理页 · 扁平单层 10 项，每项＝法人代码＋法人名称）

| 法人代码 | 法人名称 |
|---|---|
| Xuyu-test2 | 法人2 |
| 67890 | MZ测试 |
| A086440 | 富联科技(晋城)有限公司 |
| 12345 | JY测试 |
| A086582 | 富联科技（山西）有限公司 |
| A084031 | 富联精密科技公司 |
| A886022-1 | 鸿佰科技股份有限公司 |
| A886022-2 | 鸿佰科技股份有限公司-新竹 |
| A886022-3 | 鸿佰科技股份有限公司-大园 |
| A086996 | 富联裕康医疗科技(深圳)有限公司 |

### 9.4 策进组层级（策进组（包含策进组的二级组织）.png · 策进组管理页 · 两级结构）

一级 3 个策进组，各自二级组织：

- **E策进组**：E1气候行动、E2能源效率、E3环境保护、E4循环经济、E5绿色产品
- **S策进组**：S1劳动人权、S2公共发展、S3健康安全、S4化学品安全
- **G策进组**：G1商业道德-审计处、G2公司治理、G3经济与税务、G4法务合规、G5风险与机遇、G6资讯安全、G7数智化、G8价值链、G9信披治理、G1商业道德-弊弊防治处（截图截断，疑为「弊端防治处」，待核实）

> 选择规则（v5 起）：**跨维度累计勾选**——同一指标可同时勾选多个维度的多个对象，每个维度内仍多选；策进组可只勾选一级（＝覆盖该组）或勾选二级组织；选择结果只作用于当前配置的指标条目；多选返回一一对应、无累加（v5 9/7 澄清）。选中可视化（着色选中态＋按维度着色总览 chips）见 FP-7 与流程 C（v6）。

## 10. 数据口径与选项范围规则（9/7–9/8 拍板 · v7 补账同步）

> 本节两块规则于 9/7–9/8 会话拍板后先行落盘于开发文档 6.2.6 与 REQ-01 终版文档，v7（2026-09-11）抄回本 REQ 固化，作为结果表格取数与年份/维度选项范围的唯一事实源口径；**文字以开发文档 6.2.6 为准**（与终版文档的差异点见「本版变更」）。后续派生三件套时本节内容必须保留（见第 8 节 B 补记）。

### 10.1 数据口径与取数规则（结果表格「数值」列的取数依据）

1. 数据范围：取**已填报**的指标值——填报行存在且值非空即计入，**不限表单状态**（进行中／已完成均可）。
2. 取值唯一性：同一指标 × 年份 × 维度对象只存在一份填报（正式环境由收集流程保证，不出现同一指标在多张表单内），无需多份取值规则。
3. FII 整体口径：FII 行数值＝该指标该年份全部有效填报，按该指标在指标配置时已设定的汇总方式汇总合成。
4. 法人口径：＝归属该法人的全部填报行，**含该法人自己填报的行与分会代表其填报的行**（分会一次填报代表其覆盖的全部法人，系统按法人拆行落库、各行同值）。
5. 分会口径：＝该分会作为填报主体提交的填报；一次填报在结果中呈现 **1 行**，不按其覆盖的法人展开、不重复计。
6. 策进组口径：可只勾一级策进组，也可勾选一级策进组及其下二级组织的一个或多个，各自独立计行——勾选一级＝该策进组整体口径 1 行（数值取该组整体口径的填报，汇总合成同第 3 条）；勾选的每个二级组织＝该组织口径各 1 行。
7. 年份：年份下拉的值＝**表单的收集年份**（选 2025 即取收集年份为 2025 的表单数据）。
8. 无数据：该指标 × 年份 × 维度对象下无已填报行（或填报行值为空）时，保留该行、数值列以"—"占位。
9. 数值展示：定量指标显示数值，定性指标显示填报文本原样；单位取指标库中维护的单位。
10. 指标库范围：第 1 层显示所有「有效」状态的指标库。

### 10.2 年份与维度选项按指标实际收集范围裁剪（维度选择二级弹窗与年份下拉的选项范围规则）

> 优先级（沿用开发文档 6.2.6 原文声明）：本节优先于「全量列表」选项口径与「不选默认当年」口径，其余条款不变——对应到本 REQ，即优先于正文所有「默认当年（当前＝2026）」表述（FP-4、FP-6、流程 A 步骤 11/13、数据项清单「年份（指标级）」行、第 5 节「条目年份未选」行）与第 9 节附录 mock 全量列表（仅供原型 mock 取用）；v6 既有文字按"只增不改"保留原样，凡与本节冲突处以本节为准。

1. 选项范围总则：某指标条目打开维度选择二级弹窗时，四个维度页签显示的选项**不取全量**，按**该指标实际派发过的对象**裁剪——未派发给该指标的对象不显示、不可选。**年份与维度两项配置互相独立、无联动**：年份下拉仅显示该指标实际存在收集表单的年份，可不选，不选时默认取最新有收集记录的年份，**下拉占位动态显示该默认年份（如「默认 2025 年」）**；无任何收集记录时无默认年份，空态见第 5 条；年份与维度的组合查不到数据时，结果按无数据处理（"—"占位），不做勾选清理或提示。
2. 派发范围判定：以该指标历年收集表单的派发记录为准（并集）——法人页签＝该指标派发的法人（仅派发 A、B、C 三个法人时，列表只出现这 3 个，其余法人不显示）；分会页签＝该指标按分会收集的分会（仅 CESBG、SEG 两个分会收集过时，列表只出现这 2 个，其余分会不显示）；策进组页签＝该指标经策进组／中央单位实际收集的对象（例如只派发 E策进组时，页签内只出现 E策进组及其下实际派发的二级组织，S策进组、G策进组及其他策进组均不显示；一级组与其下二级组织按实际派发结构显示两级）。
3. FII 选项保留：FII 页签不受裁剪影响，永远显示「FII」1 个选项（整体口径为汇总计算，不依赖派发）。
4. 派发未填仍显示：裁剪依据是**派发范围**而非是否有填报值——已派发但尚未填报的对象仍出现在选项中（查询结果按主流程步骤 14 以"—"占位）。
5. 从未发起过收集的空态：该指标在任何年份均无收集表单（无任何派发记录）时，年份下拉无可选项，四个维度页签均显示空态提示"该指标尚未发起过收集"；此时可直接提交查询，按无数据处理（FII 1 行、数值"—"）。
