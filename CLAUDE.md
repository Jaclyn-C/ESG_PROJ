# CLAUDE.md — PM-workflow 产品经理工作台 总规则

> 项目无关的 PM 需求工作台。主对话与所有 Agent 在本仓库工作均遵守本规则。

## 1. 工作台定位

把 PM 口述的需求变成四类资产：**需求理解（唯一事实源）→ 三件套（客户确认说明 PDF / 开发需求文档 / 交互原型）**，外加汇报 PPT、调研报告、工作日志。

- 一个项目 = `projects/<项目名>/` 一个目录，可多项目并行；
- 本工作台本身项目无关，换项目不需要改任何 agent 定义。

## 2. 团队与产物

| Agent | 产物 | 格式 | 触发方式 |
|---|---|---|---|
| product-agent | 需求理解（REQ 文档）、需求总览 | md | 有需求就派 |
| confirm-doc-agent | 功能确认说明 | LaTeX→PDF | 三件套派生时 |
| dev-spec-agent | 开发需求文档（含验收清单） | md | 三件套派生时 |
| prototype-agent | 交互原型 | html | 三件套派生时 |
| ppt-agent | 汇报材料 | html 幻灯片 | **仅用户点名 + 给方向** |
| worklog-agent | 会话记录/日报/周报 + status.md | md | 收工打卡 / 点名 |
| research-agent | 调研报告 | md | 仅用户点名 |

**主对话职责**：听懂用户、找对文件、派活时把文件路径写进任务书、收产物汇总、执行三件套交叉核对（§5）、维持会话纪律（提醒打卡、发现账实不符提醒补记）。子 agent 每次都是全新上下文，不知道任何历史——路径必须由主对话递。

## 3. 项目目录结构

```
projects/<项目名>/
├─ inputs/               ← 用户放素材（agent 只读）：01-原始需求文档.pdf（序号=阅读顺序）
│  ├─ screenshots/         页面快照（原型的内容基础）
│  └─ style-refs/          风格参考（设计规范/喜欢的界面截图）
├─ understanding/        ← 需求理解：每需求一份且只有最新版 REQ-NN-名称-vN.md
├─ outputs/
│  ├─ confirm/   功能确认说明（.tex + .pdf）
│  ├─ dev-spec/  开发需求文档（.md）
│  ├─ prototype/ 交互原型（vN/ 子目录，含 index.html）
│  ├─ ppt/       汇报幻灯片（vN/ 子目录）
│  ├─ research/  调研报告
│  └─ overview/  需求总览快照（只读生成物）
├─ worklog/              ← YYYY-MM-DD.md（会话记录）+ weekly/YYYY-第N周.md（周报）
├─ status.md             ← 当前状态快照（worklog-agent 收工时全量重写）
└─ archive/              ← 被替代的旧版本（需求理解旧版、旧产物）
```

**新项目启动**：按此结构创建骨架（可参照 `projects/_template/`），初始化 `status.md`，并向用户确认项目名。

## 4. 需求理解（唯一事实源）规则

1. **一个需求一份文档**：全新主题建新 REQ-NN；同一需求反复修改只升版本（v1→v2→…），不新建。判断"是否新需求"拿不准时问用户。
2. **每个版本是全量完整版**（不是增量补丁）；头部必带「本版变更」节与状态标记（`草稿 / 客户已确认+日期 / 已废弃`）。
3. **客户已确认的版本锁死**：只能出新版，不能改历史。
4. 用户在对话中转述的客户**原话（带引号）**落盘为证据，记入本版变更节（`依据：M/D 对话，客户原话："…"`）。
5. **待确认纪律**：信息不足处一律写「待确认」反问用户，禁止编造；未确认的问题不得拍板落盘。
6. 每次升版：新版写入 `understanding/`，旧版挪 `archive/`——understanding/ 里每个需求**永远只有最新版**。
7. 模板：`templates/需求理解模板.md`。
8. **需求总览**：用户要看项目全貌时，把各 REQ 最新版聚合为只读快照（存 `outputs/overview/`，文件名带生成日期）。总览是视图不是事实源，用完不维护。

## 5. 三件套派生与交叉核对

- 确认说明 / 开发文档 / 原型从**同一份需求理解的同一版本**派生，产物版本号 = 需求理解版本号；
- 产物遇到事实源没写的点：标「⚠️ 待确认」回报主对话，不编造；
- 三件套生成后，**主对话必须交叉核对**：① 功能点数量一致 ② 页面/模块清单一致 ③ 开发文档写的每条交互步骤能在原型中点到。不一致 → 回对应 agent 修正后再交付。

## 6. 会话与状态纪律

- **开场**：新会话第一件事读当前项目 `status.md`；用户未指明项目时，找 status.md 修改时间最近的项目向用户确认。
- **收工打卡**：用户说「收工 / 记一笔 / 就到这」→ 主对话立即调 worklog-agent：记会话记录 + 全量重写 status.md。
- **补记**：新会话发现账实不符（如 understanding/ 出现无记录的新版本文件）→ 用文件时间戳推断发生日期，提醒用户补记；补记按**事情发生日期**入账，不按补记日期。
- **切换项目**：「切到 XX 项目」→ 读该项目 status.md 后继续。
- 多轮工作同一天：worklog 当天文件追加多条，互不覆盖。

## 7. 修改规则

| 改动性质 | 做法 |
|---|---|
| 功能/交互/数据变化 | 对话说明 → product-agent 升需求理解 → 重派生受影响产物 |
| 错字/措辞/样式微调 | 用户直接改文件，改完在对话里说一句由 agent 同步进需求理解 |
| 原型的一切 | 直接改或对话改均可（增量迭代，手改会被继承） |

**禁止**绕过需求理解直接改三件套的实质内容——下次派生会被静默覆盖。

## 8. 保密红线

1. 一切客户内容默认**不出本机**；仅用户主动复制出去除外。
2. research-agent 联网搜索**只带公开关键词**，严禁把客户文档原文、公司名、内部数据放进搜索词。
3. 除 research-agent 联网检索外，任何 agent 不得访问网络。
4. 不得虚构"客户说过"的内容。

## 9. LaTeX 交付线（客户确认说明）

- 使用 `templates/confirm-doc.tex` 模板；编译用 tectonic 或 xelatex，PDF 交付客户，`.tex` 源文件一并存档；
- 编译报错由 confirm-doc-agent 自行修复迭代直至成功，如实报告；
- 编译环境未安装时：生成 `.tex` 并明确告知用户「源文件已就绪，安装 tectonic 后一条命令出 PDF」，不阻塞其余产物。

## 10. 禁止事项

1. 禁止在 `understanding/` 留存同一需求的多个版本（旧版必须进 archive/）。
2. 禁止未经用户确认将需求理解标记为「客户已确认」。
3. 禁止替用户拍板待确认问题。
4. 禁止为用户打包、压缩、发送任何文件（分享由用户自己做）。
5. 禁止在 worklog 记录与工作无关的闲聊；判断标准：这轮改动值不值得进周报。
6. 禁止 ppt-agent 在用户未点名时被自动调用。
