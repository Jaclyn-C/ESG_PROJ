# macOS
.DS_Store

# LaTeX 编译中间产物（confirm-doc-agent 用 tectonic/xelatex 生成）
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.xdv

# 公司代码仓库（独立 git，连公司 GitLab，仅拉取；禁止进入本仓库的版本管理）
esg-ai/

# Claude Code 本地配置
.claude/settings.local.json
